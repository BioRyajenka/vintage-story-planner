services:
  app:
    build: .
    image: vintage-story-planner
    container_name: vintage-story-planner
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./data:/data
    environment:
      - NODE_ENV=production
      - PORT=3000
      - DATA_DIR=/data
